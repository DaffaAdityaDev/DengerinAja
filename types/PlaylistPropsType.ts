type PlaylistPropsType = {
    id: number,
    title: string,
    artist: string,
    album: string,
    url: string,
    img: string,
    slug: string,
    playing: boolean,
}

export default PlaylistPropsType;